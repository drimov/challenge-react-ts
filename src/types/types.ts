export type Theme = {
  backgroundP: string
  backgroundSd: string
  foreground: string
  iconColor: string
}

export type ChildrenProps = {
  children: React.ReactNode
}
export type ThemeString = 'light' | 'dark'
